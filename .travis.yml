language: node_js
node_js:
  - "6"
before_script:
  - yarn run bootstrap
script:
  - echo "$(git log master.. --pretty=%B)"
  - yarn run commitmsg -- "$(git log master.. --pretty=%B)"
  - yarn run build
  - yarn run lint
  - yarn test -- --runInBand
