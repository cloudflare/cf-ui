language: node_js
node_js:
  - "6"
before_script:
  - yarn run bootstrap
script:
  - yarn run build
  - yarn run lint
  - yarn test -- --runInBand
deploy:
  provider: script
  script: yarn run publish -- --yes
  on:
    branch: master
