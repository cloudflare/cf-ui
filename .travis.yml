language: node_js
node_js:
  - "node"
  - "6"
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
before_script:
  - yarn run bootstrap
script:
  - yarn run build
  - yarn run lint
  - yarn test
