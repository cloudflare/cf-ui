export default baseTheme => ({
  color: baseTheme.colorDarkGray,
  fontStyle: baseTheme.fontStyle,
  fontSize: baseTheme.fontSize,
  borderRadius: baseTheme.borderRadius,
  spinAnimation: baseTheme.spinAnimation
});
